package routes

import (
	"github.com/gin-gonic/gin"
)

// 注册路由列表
func ApiRouter(router *gin.RouterGroup) {
	/*
	// 消息
	router.GET("/message/index", controllers.MessageIndex)
	router.POST("/message/store", controllers.MessageStore)
	router.PUT("/message/update", controllers.MessageUpdate)
	router.DELETE("/message/delete/:id", controllers.MessageDelete)

	// 用户
	router.GET("/user/index", controllers.UserIndex) */
}